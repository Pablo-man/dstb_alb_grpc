# Project Name

A brief description of what the project does and why it is useful.

## Characteristics

- Brief description of the main characteristics of the project.
- Example: "This project provides a service for basic mathematical operations using gRPC."

## Technologies used

- **Python** 3.x
- **gRPC**
- **Protobuf**
- Other important dependencies...

## Facility

### Prerequisites

1. Python 3.x installed.
2. The necessary Python dependencies.

### Steps to install

1. **Clone the repository:**

   ```bash
   git clone https://github.com/your-user/your-repository.git
   cd your-repository

2. **Install dependencies**
    ```bash
    pip install -r requirements.txt

    ```
3. **Run the gRPC server**
    ```bash
    python calculator_server.py

    ```
4. **Run the client**
    ```bash
    python calculator_client.py

    ```